[package]
name = "hello-world"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
wit-bindgen = { version = "0.54", features = ["async-spawn"] }
ciborium = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib"]

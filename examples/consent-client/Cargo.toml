[package]
name = "consent-client"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
act-sdk = { path = "../../act-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "1"

[lib]
crate-type = ["cdylib"]

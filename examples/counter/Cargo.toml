[package]
name = "counter"
version = "0.1.0"
edition = "2024"

[dependencies]
wit-bindgen = { version = "0.53", features = ["async-spawn"] }
ciborium = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib"]

[package]
name = "hello-sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
act-sdk = { path = "../../act-sdk" }
wit-bindgen = { version = "0.53", features = ["async-spawn"] }
serde = { version = "1", features = ["derive"] }
schemars = "0.8"

[lib]
crate-type = ["cdylib"]
